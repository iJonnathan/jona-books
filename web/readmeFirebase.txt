1. npm install -g firebase-tools

2. firebase login

3. firebase init
    seleccion el proyecto firebasecloud
    agrego el path del proyecto compilado: dist
    completo todos los requerimientos con yes, no, yes(overwrite)
4. npm run build

5. firebase deploy